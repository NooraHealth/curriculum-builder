import { Meteor } from 'meteor/meteor';

import '../imports/api/curriculums';
import '../imports/api/lessons';
import '../imports/uploads/image';

Meteor.startup(() => {
  // code to run on server at startup
});
